# Portfolio Visitor Analytics & Email Notification Credentials
RECIPIENT_EMAIL=gsampathchary454@gmail.com

# SMTP Server Configuration (Optional - for sending emails via Gmail / custom SMTP)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Cron Trigger Secret (Optional - for automated Vercel Cron or GitHub Action triggers)
# CRON_SECRET=your_random_cron_secret_key
